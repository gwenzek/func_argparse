from setuptools import setup


setup(
    name="func_argparser",
    version="1.0.0",
    description="Generate CLI ArgumentParser from a function signature.",
    author="gwenzek",
    packages=["func_argparser"],
    install_requires=[],
)
